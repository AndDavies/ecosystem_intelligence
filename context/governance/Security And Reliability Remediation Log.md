# Security And Reliability Remediation Log

Status: active operating register

Owner: Andrew Davies

Last reviewed: 2026-09-05

This register records security, privacy, resilience, dependency, and operational findings that require either a verified repair or an explicit disposition. It complements the release runbook and access matrix. It must not contain credentials, private evidence, raw provider payloads, personal data, or exploit details that would increase public risk.

## September 5 audit disposition

The full repository/security/skill audit was reconciled with the concurrent Signals, Research and Visibility work. Seven demonstrated boundaries are repaired in the local baseline candidate: pre-provider reservation integrity, request-identity stability, member submission state, quota timestamps, auth return validation, spreadsheet serialization and outbound source-image transport. Direct route/database tests and one independent patch-review cycle cover malicious and legitimate behavior. Working List text bounds, complete reads/exports, bounded transient fallback, explicit type checking and dependency updates accompany the repair. The two additive migrations were applied on September 5 through the linked CLI. Andrew authorized the application push without further testing. The prior direct-role/route evidence remains the validation checkpoint; release completion uses exact deployment status, and post-deployment functional checks were explicitly waived.

Read-only production advisors on September 5 report two authenticated security-definer warnings for the canonical-repair Review and Publish RPCs. Their bodies check staff status and bind the supplied reviewer to `auth.uid()`; authenticated grants are intentional for the existing human workflow. Twelve informational RLS-without-policy notices describe deny-by-default service-only tables. Performance notices comprise two unindexed foreign keys, 36 unused indexes and absolute Auth connection allocation. Preserve required/young indexes and record workload evidence before structural tuning; these notices are not verified exposure findings. The local dependency audit is clean at all severities.

## Severity and release rule

- **Blocker**: repair and regression-test before broader promotion.
- **High**: schedule immediately; do not defer if the condition is externally exploitable or repeatedly harms a primary journey.
- **Medium**: tracked hardening with a named verification trigger.
- **Low**: defence-in-depth or operational polish.
- **Accepted**: deliberately retained with a documented boundary and re-review trigger.

Resolved means the source repair and its regression evidence are complete. Deployed means the repaired commit has also passed the production deployment and smoke checks.

## Launch-blocking findings

| ID | Finding | Root cause | Repair | Verification | Status |
| --- | --- | --- | --- | --- | --- |
| SEC-2026-001 | Production dependency audit reported 24 high, 31 moderate, and five low findings; the later full graph still contained development-tool findings | Next.js 15.5.15, a nested Sharp 0.34.3 copy, vulnerable transitive PostCSS and WebSocket versions, and affected development-tool transitive packages | Updated Next.js and its ESLint contract to 15.5.22, Sharp to 0.35.3, moved `shadcn` to development-only, pinned patched runtime transitives, upgraded Vitest, and constrained affected Vite, Hono, YAML, URI, query-string, body-parser, address, glob and build-tool transitives | `pnpm security:validate` audits the complete production and development graph and reports no known vulnerability; Node 24 release validation passed 250 tests, lint, the 5,000-marker scale gate, type checking, and the production build; GitHub Release Validation and CodeQL passed with zero open code-scanning and dependency alerts | Deployed and verified |
| REL-2026-001 | A capability route produced a transient production 500 while loading public citations | Every dossier originally loaded all public citations. Production verification then showed the citation RLS policy could still time out while checking scoped IDs. | Build citation targets only from published organizations, technologies, reviewed matches, funding events, and public needs. Hydrate those exact IDs through the server-only database client, require public-approved evidence and sources, and process bounded batches sequentially. The service credential never leaves the server. | Targeted loader tests, repeated public profile requests, canonical launch crawl, and current production health and route smoke | Deployed and verified |
| PRIV-2026-001 | Public organization responses exposed internal demand-match reviewer rationale | The public `AtlasDemandMatch` model reused the database editorial field and Ask True North included it in its catalogue and search text | Removed rationale from the public database projection, public TypeScript contract, deterministic search corpus, AI catalogue, and every public serializer that derives from the shared contract; retained private admin/candidate rationale separately | Contract tests, public API inspection, Ask catalogue test, exports and route regression, and public response scan | Deployed and verified |
| SEC-2026-008 | The complete release dependency gate detected newly disclosed high-severity advisories in transitive `js-yaml` and `nanoid` versions | The workspace still constrained `js-yaml` to 4.3.0 and the PostCSS toolchain resolved `nanoid` 3.3.16 after patched releases became available | Pin the patched transitive versions `js-yaml` 4.3.1 and `nanoid` 3.3.17 through the workspace override contract without changing application behaviour | Complete dependency audit, tests, lint, production build, GitHub Release Validation, CodeQL, and production health smoke passed for commit `783b7f5` | Deployed and verified |
| SEC-2026-009 | The August 17 release gate detected newly published high-severity `nanoid` advisory `GHSA-2v37-7h3g-55p8` in the previously patched 3.3.17 transitive override | The upstream advisory was updated after the earlier release and now identifies every 3.x version before 3.3.18 as vulnerable to a zero-size custom-generator infinite loop | Advance only the existing transitive override and lockfile to `nanoid` 3.3.18 without changing its PostCSS/Next consumers | Complete dependency audit must return zero high/critical findings, followed by tests, lint, scale validation, clean production build, exact-deployment gate and GitHub security checks | Repaired locally; production deployment verification pending |
| REL-2026-005 | Approved refresh candidates could not be published | The precision guard called a deliberately private helper while executing as the authenticated reviewer, so PostgreSQL denied the helper before the atomic publication began | Inlined the immutable before-record timestamp extraction inside the guarded public publication function without granting access to the private helper or changing publication authority | Versioned migration is applied in production; full release validation and human publication previously confirmed the repaired permission boundary | Deployed and verified |
| REL-2026-007 | Validated refresh candidates could not enter private Admin Review | The trusted service-role staging RPC inserted through a precision trigger that invoked the private immutable baseline parser, but `service_role` lacked execute permission on that parser; the transaction rolled back before creating a candidate | Granted only `service_role` execute on the private parser while keeping `public`, `anon`, and `authenticated` denied; no writer or publication authority changed | Live privilege matrix, guarded RPC result `staged_count=1`, published North Vector refresh lineage, `pnpm research:validate`, migration tests, and the complete `pnpm release:validate` gate | Deployed and verified |
| REL-2026-009 | The first production dossier crawl caused rich-view REST 500 responses and PostgreSQL statement timeouts for unactivated legacy organization profiles | The shared slug loader queried the correlated `organization_dossiers` view before checking `editorial_profile_version`, so every legacy profile paid the rich projection cost and the public-read retry could repeat a timeout | Resolve the published organization identity and exact profile version first; query the rich view only for `organization_editorial_profile_v1`, restore the bounded legacy table loader otherwise, and advance the cache key | Hotfix `8a29b13`; 305-test Node 24 release gate; all 11 affected routes; 869-page zero-warning crawl; no hotfix-deployment 5xx/runtime error or post-hotfix PostgreSQL timeout; unchanged health, catalogue, candidate and activation state | Deployed and verified |
| REL-2026-010 | A schema-valid dossier pilot entered private Admin Review with duplicated, generic reviewer text and misleading `0 fields reviewed` operation cards | The ignored pilot builder substituted organization names into shared templates, the tracked 1.6 contract validated shape rather than cross-artifact specificity, and the Review page treated scalar values like objects while hiding some relationship fields | Advance new runs to the complete same-run 1.7 record-specific gate, verify staging payload parity at import, align the ignored operator skills, regenerate and guarded-restage the eight pending candidates, and use one readable shared operation renderer plus separate generated and reviewer-decision rationales | Deterministic artifact hashes, 1.7 negative/positive contract tests, complete research validation and smoke, Admin operation tests and responsive preview, guarded 8/0 restage, zero decisions/activations, Node 24 release gate, and production Admin/auth/log verification | Deployed and verified |
| REL-2026-011 | Geography rendered as a broken image on every activated organization dossier, and the national map depended on a warm browser cache | The deployed public MapTiler key was non-empty but provider-invalid. The dossier used a MapTiler Static API URL through Next Image, producing an optimizer 502 after the upstream 403, while the shared interactive map selected MapTiler on key presence alone. | Replace the static image with the lazy shared renderer on a fixed OpenStreetMap base; preflight MapTiler style availability before the interactive atlas selects it; fall back to OpenStreetMap on rejection or network failure; retain the ordinary selected-record map link and the text-only branch for missing or unverified coordinates. | Provider-resolution unit tests, dossier contract tests, YMX plus a second activated dossier and a legacy profile, deliberate invalid-key `/map` browser regression, complete map/launch/release gates, and production route/log verification | Deployed and verified |
| REL-2026-012 | Publishing the seven first-wave dossiers produced a five-minute visible failure on `/organizations` and `/map` | Batch publication purged a shared discovery/detail cache tag and every organization-detail route. Default Next link prefetch then rewarmed unrelated rich dossiers alongside 15 compact discovery reads until anonymous PostgreSQL statements timed out; transaction-aborted errors were secondary. | Separate national discovery and exact organization cache tags, invalidate only changed slugs during ordinary publication, disable speculative organization-dossier prefetch across listing surfaces, retain a bounded discovery TTL and explicit owner-only global flush, and retry the transaction-aborted follow-on as transient. | Exact-tag/path and prefetch regressions, Node 24 application/research/launch/scale/release gates, 870-page crawl, live 415/15/400 reconciliation, responsive directory/map/dossier smoke, and a clean 7.5-minute post-deployment Vercel/PostgreSQL timeout window | Deployed and verified |
| REL-2026-013 | The public dossier view still performs a nested citation aggregate before the application can bound evidence to the requested record graph, creating a cold-path timeout and RSC-stream risk as dossier depth grows | Citation aggregation lives inside the security-invoker dossier view even though the server already knows the published organizations and reviewed child IDs it is allowed to expose | Keep the compatibility `citations` member empty, remove the nested aggregate, admit the public dossier graph first, hydrate only its approved field citations/evidence/sources, and make one evidence/citation ledger feed the organization and capability dossier family | Migration/RLS and bounded-query tests; at least ten exact-deployment cold dossiers spanning high-citation, sparse, recently updated and coverage-fill records; anonymous view/API/route, RSC/loading/metadata, non-zero citation trail, forbidden-lineage absence, default view p95 below 500 ms and API p95 below 2,500 ms; clean runtime/Postgres log window | Deployed; view split and bounded hydrator active. Exact-deployment cold gate and log checks are release acceptance evidence. |
| PRIV-2026-002 | Internal research and review lineage keys can persist inside the public `organizations.profile_data` JSON object even when application views do not need them | Historical candidate/publication paths placed workflow metadata beside type-specific public fields in one public column, and the public projection did not enforce an explicit allowlist | Project only role-specific public profile fields plus approved public contact data; remove `reviewed_candidate_id`, `reviewed_by`, `research_schema_version` and `ingestion_batch_id` from existing public JSON; guard future writes; retain canonical lineage in private candidate, run, decision and audit tables | Public serialization tests, migration trigger/constraint and access tests, anonymous API/page/export/Ask scans, exact pre/post affected-row count, security advisors and production smoke. Apply only after the allowlisted application is ready because deleted JSON keys are not recoverable from the public row. | Closed 2026-08-13: 407 rows sanitized with `updated_at` preserved, zero queued refresh overlap, forbidden-key counts zero, application allowlist plus DB guard active. |
| REL-2026-014 | HTTP 200 pages can still hide RSC error digests, unresolved streamed loading boundaries or dynamic-metadata failures, and sitemap-only assurance does not prove all public navigation or intentionally cited durable sources | The bounded gate classified mainly final status/metadata, while the full audit seeded primarily from the sitemap and treated outbound links as an undifferentiated surface | Inspect buffered HTML for internal Next/RSC failure states; normalize and visit every discovered same-origin navigation target once with referrers/redirects; probe only explicitly marked durable sources within a separate ceiling; block confirmed broken sources and report redirects, bot restrictions and transport uncertainty separately | Focused release-gate fixtures, normalization/deduplication/host-safety tests, bounded exact-deployment gate, one explicitly triggered paced full audit with persisted report, and a clean Vercel/Supabase error window | Core release repair closed 2026-08-13. The complete internal traversal checked 1,117 sitemap pages, 24 supporting pages and 3,002 normalized internal targets with zero operational failure or recovered warning. It then stopped safely before classifying 1,817 marked sources because the old ceiling was 1,500. The deployed real-corpus ceiling is 2,500 and the body budget now applies only to HTML. Owner-directed outbound classification is deferred to a manually initiated off-hours assurance run; it is not an ordinary release gate or an unapplied product repair. |
| REL-2026-015 | The new executive decision snapshot and North Signal CTA stage would fail closed or misreport if application code ran before their database contracts | `executive_relevance_summary`, reviewed publication functions and `newsletter_cta_click` are versioned schema changes | Apply additive migrations `20260813081430` and `20260813081500`, deploy the compatible application, then apply `20260813081542` and baseline-preserving cleanup `20260813083552`; require Review/Publish and telemetry tests; do not stage pipeline 1.7.3 or emit the CTA event until deployed contracts are compatible | Repository/live-ledger comparison, clean migration chain, preserved refresh baselines and queue-ID count, 1.7.2 backward compatibility, 1.7.3 null/supported/missing-citation cases, new/refresh Review and Publish tests, CTA raw-ledger and QA-filter scorecard tests, exact deployment smoke | Closed 2026-08-13: ordered two-stage migration complete, live ledger aligned and production contract advertises 1.7.3. |
| REL-2026-016 | A valid MailerLite unsubscribe reached the production webhook with HTTP 202 but the local consent row remained subscribed | MailerLite's live lifecycle delivery used a signed `{ events: [...] }` envelope with nested `subscriber` records; the parser accepted only a flat object or top-level array and therefore ignored the authenticated batch | Normalize flat, array and provider batch-envelope payloads; read subscriber identity/status from the nested record; treat subscriber event type as authoritative when the embedded status is stale; withdraw local consent when normalized provider status is unsubscribed | Provider-shape parser regression, complete Node 24 release gate, exact production deployment, live branded welcome and provider lifecycle checks, zero active release-QA subscribers | Closed 2026-08-13: repaired application is deployed; two QA records are retained as unsubscribed audit rows and contribute zero active subscribers. |

## Open hardening register

| ID | Priority | Finding and impact | Recommended repair | Verification trigger | Status |
| --- | --- | --- | --- | --- | --- |
| SEC-2026-002 | Medium | The CSP still requires `unsafe-inline` for scripts and styles, reducing protection against a future injection defect | Adopt nonce- or hash-based script policy incrementally, validate Next.js and approved providers, then remove the unsafe directive where compatible | CSP report-only trial followed by browser regression | Planned |
| SEC-2026-003 | Low | Dormant Microsoft Clarity hosts remain in the CSP even though Clarity is intentionally not part of the launch stack | Remove Clarity hosts from the active allowlist; add them back only with a reviewed consent-bound activation | Header snapshot and consent regression | Planned |
| SEC-2026-004 | Low | Responses disclose `X-Powered-By: Next.js`, providing unnecessary framework fingerprinting | Set `poweredByHeader: false` | Production-header check | Planned |
| SEC-2026-005 | Medium | `public.upsert_defence_brief` is a security-definer RPC executable by authenticated users; internal staff and reviewer checks currently fail closed | Move the operation behind a server-only admin path or revoke broad authenticated execute while preserving the exact administrator workflow | Anonymous/member/admin matrix plus brief save/publish regression | Planned |
| SEC-2026-006 | Closed | Supabase leaked-password protection had been disabled while the public login accepted only Google OAuth and passwordless email | Enable the provider control without introducing a password flow; retain the existing OAuth/passwordless public experience | Current Supabase security-advisor read plus sign-in regression | Closed 2026-08-12: leaked-password protection is enabled and the prior warning is absent from the current security-advisor result. |
| SEC-2026-007 | Medium | Authenticated submissions accept a flexible record payload and connection/submission endpoints need explicit per-account quotas | Add allowlisted fields, maximum serialized size and nesting depth, and daily per-user limits in addition to platform body limits | Malformed, oversized, repeated-request, and valid-flow tests | Planned |
| DEP-2026-001 | Medium | Dependabot reported four non-high alerts. Runtime PostCSS 8.5.18 alert #37 (`GHSA-fxqj-rqcc-2cmp`) could read an arbitrary `.map` file when an attacker-controlled source-map directive was processed without a bounded source path. Development-only Hono 4.12.27 carried alerts #39 (`GHSA-54fx-42gc-7vw4`), #40 (`GHSA-79qm-7rj5-m7r9`) and #41 (`GHSA-f23p-vx2j-j53r`), plus the complete graph exposed the related CORS ReDoS advisory. | The workspace overrides, hardening assertion, and lockfile pin PostCSS 8.5.23 and Hono 4.12.34 without changing their direct consumers. PostCSS remains shared by Next.js and build/test tooling. Hono remains confined to the development shadcn/MCP toolchain and is not imported by the production application. | Preserve the patch-only overrides, exact-version regression, and focused shadcn compatibility check; do not broaden the dependency upgrade. | Complete production/development dependency audit, tests, lint, build, CodeQL, production smoke, and GitHub Dependabot, code-scanning and secret-scanning alert reads. | Closed 2026-08-13: release audit reports zero known vulnerabilities; exact-commit Release Validation and CodeQL passed; GitHub reports zero open Dependabot, code-scanning and secret-scanning alerts. |
| REL-2026-002 | Medium | Multiple open tabs can race refresh-token rotation and create expected `refresh_token_already_used` telemetry noise | Deduplicate the client auth-state check or add a cookie-presence fast path while keeping server authorization on `getUser` | Multi-tab sign-in/sign-out test and clean runtime logs | Planned |
| REL-2026-003 | Medium | The anonymous homepage returns private no-store HTML, limiting CDN resilience during a traffic spike | Separate the cacheable public shell/data from the client auth-state request without changing signed-in navigation | Cache-header, auth-state, personalization, and performance regression | Planned |
| REL-2026-004 | High | The launch crawler still combined an ordinary release gate with an exhaustive sitemap/SEO/link/performance audit. An August 11 run against 872 routes produced 145 derivative findings and 13 retry warnings while Vercel/Supabase recorded a concurrent 16:18–16:42 UTC burst of truncated upstream JSON, connection timeouts and 522 responses. The run retained no detailed report and could not distinguish a candidate defect from pressure created by the assurance tool. | Split `launch:validate` into an exact-deployment gate covering operational endpoints, five core routes and only explicitly affected paths. Dynamic-family representatives are opt-in for shared renderer, metadata, navigation or family-contract changes. Move the full sitemap inventory to `launch:audit`; serialize it with a 750 ms minimum plus jitter, acquire and heartbeat an owned target-origin lock across chats/worktrees, identify each run, persist progress to a temporary report, stop on repeated final or recovered pressure and degraded health, skip metadata parsing for error/non-HTML bodies, and keep SEO/orphan/duplicate/performance findings distinct from HTTP/operational blockers. Correct retry wording so recovery is reported only after the retry succeeds, allow one advisory recovery in the bounded gate, and block repeated recovery. | Pure origin-rewrite/route-selection, redirect-boundary, authorization, lock/pressure and blocker-classification tests; retry-success and retry-failure fixtures; bounded production gate against the exact release SHA; responsive affected-route smoke; clean post-ready Vercel/Supabase error window; and a later explicitly authorized full audit with its persisted report | Resolved; the bounded exact-deployment gate and live log check remain required for each public release. Production full-site assurance is explicit-only through `$tnm-site-assurance`, requires acknowledgement plus an approved reason, and never runs merely because code was pushed. |
| REL-2026-017 | High | The separate private visibility refresh retained a second implicit full-sitemap crawler. On September 4 an expired legacy snapshot caused it to fetch 1,265 production routes with concurrent layered retries, coinciding with Supabase 522/525 responses, statement timeouts and slow public routes. | Keep full sitemap retrieval as manifest validation only; inspect exactly `/`, `/organizations`, `/map`, `/signals`, and `/north-signal` sequentially with delayed failed-route retry and a three-pressure-signal circuit breaker. Retire `--refresh-technical`, make full traversal exclusive to guarded `$tnm-site-assurance`, and suppress technical deltas across unlike scopes or URL sets. | Pure 1,265-URL selection test proving five outputs, scope/URL-set comparison regressions, visibility/launch focused tests, installed-skill validation, Node 24 release gate, post-ready bounded launch validation and a clean production health/log window | Resolved in the September 4 release candidate; no full-site crawl is part of its verification. |
| REL-2026-006 | High | A genuine stale refresh previously raised PostgreSQL `40001`, a retry-class serialization error. The July 29 failed publication produced a rapid repeated-error burst immediately before the Supabase high-CPU warning. | The guarded historical publisher and the dossier v2 publisher now use non-retryable review-state SQLSTATEs for stale parent, stale child, and canonical-program conflicts while retaining reviewer-facing messages. | Controlled stale parent and child candidates reject once in the isolated publication contract; the migration source rejects `40001`; the post-deploy application and PostgreSQL log gate is clean | Deployed and verified |
| OPS-2026-001 | Medium | Completed research, visibility and editorial tasks accumulated as uncommitted paths because the originating tasks had no commit authorization | Reconciled each path to live state, retained validated immutable lineage and reusable tooling, removed obsolete collateral, and kept explicit path staging plus the prohibition on `git add .` | Worktree and diff review before every release | Reconciled 2026-08-05; active control retained |
| OPS-2026-002 | Low | July 19 test submissions, connection, and contact records pollute administrator launch metrics | Close or label the known fixtures through the ordinary admin workflow; preserve audit history | Live queue review | Planned before outreach |
| OPS-2026-003 | Low | Legacy screenshots and demo media remained tracked near the approved broader-beta kit | Superseded launch exports were removed from the tracked release surface and historical governance was isolated under `context/archive/governance/` | Repository path review and current launch-package link review | Deployed and verified |
| OPS-2026-004 | Low | A standing broader-release packet could become stale and be mistaken for current production | Retire tracked generated collateral; create it only on explicit request and validate against production immediately before use | Repository hygiene validation and release-owner review | Reconciled 2026-08-05 |
| ACC-2026-001 | Low | Mobile navigation did not expose the active route with `aria-current`, connect its control and menu, or close and restore focus on Escape | Match the desktop active-route contract, bind `aria-controls`, provide 44-pixel targets, and restore focus after Escape | Focused navigation contract tests plus mobile keyboard and screen-reader-oriented browser smoke | Closed 2026-08-13: shared header contract deployed with active-route state, control relationship, Escape close, focus return and target sizing. |
| COPY-2026-001 | Low | “Source-backed fact” and “Public-source fact” both appear in the public evidence language | Select one canonical public label and update the presentation dictionary and legend only | `publicLanguage` dictionary, public-copy test, local route scan, and post-deployment launch crawl | Deployed and verified |
| COPY-2026-002 | Low | The homepage public-evidence eyebrow can render without a visible word space in the accessibility tree | Correct the presentation markup without changing the section content | Desktop/mobile snapshot and public-copy test | Planned |
| ASSURANCE-2026-001 | Medium | Current assurance is strong application testing and configuration review, not an independent penetration test or complete DAST program | GitHub CodeQL, full dependency auditing, Dependabot vulnerability alerts, secret scanning and push protection are configured without automated update branches. Complete the authenticated Codex Security review, add an SBOM, and commission an independent assessment when audience, sensitivity, or organizational adoption warrants it. | Codex Security authentication and scan; broader institutional adoption or material architecture change for external testing | Repository Codex Security audit completed September 5; local repairs await deployment. Independent external assessment remains a future adoption-triggered control. |
| PERF-2026-001 | Medium | Supabase previously reported overlapping permissive RLS policies for public-read and staff-manage paths. The overlap was not a data-exposure finding but added avoidable policy evaluation. | Consolidated each affected public and staff policy while preserving the original row predicates and role boundaries. | Current Supabase advisors report no overlapping-policy lint; migration and access-matrix regressions preserve the intended reads and writes. | Deployed and verified |
| PERF-2026-002 | Low | The current Supabase performance advisor reports only informational unused-index findings plus the separately accepted absolute Auth-connection allocation. The unused set is live operational evidence, not proof that an index is redundant; young or low-volume workflow indexes can remain correct before they record a scan. | Read the current advisor set rather than preserving a fixed count, observe usage across a representative launch window, classify each index by required workflow, and remove only proven redundancy through a reviewed migration. | Thirty days of production use or a sustained CPU/query-latency issue | Monitor; no deletion authorized |
| PERF-2026-003 | Low | Supabase Auth uses an absolute database-connection allocation. Increasing the compute tier alone would not proportionally expand Auth connections. | Change to percentage allocation only when an instance-size or authentication-volume change is planned, then rerun sign-in and account regression. | Compute-tier change or sustained authentication pressure | Accepted with trigger |
| PERF-2026-004 | High | A single unpaged Data API discovery read can stop at the server row limit and silently omit organizations as the corpus grows; the legacy Leaflet fallback also grouped nearby markers with quadratic scans | Page every discovery relation in deterministic 1,000-row ranges, cache each bounded page independently under the shared `atlas-public` tag, derive collection counts from the assembled compact snapshot, group fallback markers by a linear-time grid, and require a 5,000-marker scale gate before release | Local paging, cache-item, invalidation, count-integrity, clustering, scale, build, browser regression, and production summary/atlas/complete-marker consistency | Deployed and verified |
| OPS-2026-005 | Low | A private newsletter-subscription export and superseded generated PDF and launch assets were tracked in the public repository history | Remove them from the current index, ignore private and generated paths, retain no standing generated launch kit, and assess history rewriting separately because it is destructive and disrupts every clone | Current-tree secret/private-data scan, repository hygiene gate, and explicit owner decision before any history rewrite | Current tree repaired; historical copy remains in Git history |
| OPS-2026-006 | Low | Repository migration filenames had drifted from the applied Supabase migration-version ledger even though the SQL content and live database were already reconciled | Rename local migration and rollback files to their exact applied production versions and update tests and logs without executing SQL | Repository-to-live ledger comparison plus full migration-chain tests | Deployed and verified |
| REL-2026-008 | Low | Middleware matched broad public catalogue traffic even though most routes did not require authentication refresh or legacy-route handling | Restrict middleware to the root compatibility bridge and protected account, collection, contribution, connection, and administrator routes | Public and protected-route regression, auth return paths, and production middleware logs | Deployed and verified |
| PERF-2026-005 | Low | Vercel traffic showed repeated organization-directory requests, but available logs did not establish whether they were users, crawlers, or abusive automation | A production-only, IP-scoped `/organizations` GET rate observation rule is staged in Vercel with `log` action at 120 requests per minute. Owner review and explicit Firewall publication are required before it becomes active. | Review the staged rule diff and production observations; do not switch to blocking without evidence | Draft staged; not active |

## Intentional advisor dispositions

- Supabase tables used only through the service role may show `rls_enabled_no_policy`. This is an intentional deny-by-default posture when anonymous and authenticated roles have no grants. Recheck grants whenever a route is added.
- Microsoft Clarity remains deferred. Its absence is not a defect and does not block launch.
- The private retention scheduler is required to honour the published 30-day detailed-event and 90-day raw-search limits. Any rollback must unschedule the job before dropping its private function; this dependency is agent-owned in the versioned rollback contract.

## Required recurring checks

Before a production release or broader promotion:

1. Run `pnpm security:validate`.
2. Run `pnpm release:validate`.
3. Inspect the public organization API for internal reviewer fields.
4. After the exact deployment is ready, run the bounded `pnpm launch:validate` gate and inspect recovered retries plus final failures. Run the serialized full `pnpm launch:audit` only after Andrew manually invokes `$tnm-site-assurance` for a major-information-architecture, manual-periodic-assurance, explicit-broad-audit or systemic-diagnosis purpose.
5. Review Vercel build and runtime logs.
6. Review Supabase security and performance advisors and the anonymous/member/admin access matrix.
7. Record the deployed commit and rollback target in the release runbook.
8. Compare `/api/atlas/summary`, `/api/atlas?page=1&pageSize=18`, and the returned complete marker collection; any count divergence blocks promotion.

## Audit history

- **2026-08-10 dossier review-integrity closure:** the first eight-profile enrichment packet exposed a private review-quality failure even though its public leaf evidence was structurally valid. Review and publication were stopped before any human decision or canonical write. Pipeline 1.7 now hard-fails incomplete same-run lineage, generic reviewer prose, duplicate or missing target outcomes, unresolved recovery attempts, ineligible or cross-subject evidence, cross-target or wrong-outcome refresh signals, and staging drift; Admin Review presents one generated rationale, one editable decision rationale and readable scalar, object, array, null and relationship changes. The final frozen same-ID packet (`generatedAt 2026-08-10T09:47:10.618Z`) was guarded-restaged as eight pending rows with zero skips, decisions, baseline drift or profile activations; the run has five staging audit events in total. Commit `7fbcc9f` / deployment `dpl_FF778tjCMGeMbcsDa7GaVZFGErSA` passed the Node 24 333-test release gate, GitHub Release Validation and CodeQL, authenticated Admin Review at 1440 and 390 pixels, health/count reconciliation, an 870-page crawl with zero findings or recovered warnings, and a clean Vercel/PostgreSQL post-readiness log window. The Supabase API-log connector was unavailable during the final sample, so closure relies on the clean route, Vercel and PostgreSQL evidence rather than claiming that feed was inspected. The live queue remained eight pending rows with eight matching baselines and zero decisions, publications or activations.
- **2026-08-10 release prewarm observation:** the first pre-release atlas prewarm received a Vercel 500 with a terminated upstream discovery read on `/api/atlas`; `/api/landing` briefly showed the same condition. Health and summary remained 200, PostgreSQL recorded no matching statement timeout, direct atlas and landing reads recovered, and the immediate paced 869-page rerun completed with zero findings or recovered warnings. This is retained as release evidence rather than hidden by the successful rerun; any recurrence during deployment verification blocks closure under `REL-2026-004`.
- **2026-08-09 dossier release preflight:** production was healthy on commit `783b7f5`, the public catalogue reconciled at 415 organizations with 18 bounded rich results and 415 complete markers, and the live migration ledger ended at `20260803142218`. Security advisors retained five intentional deny-by-default table notices and the accepted leaked-password warning; performance advisors reported 19 unused indexes and no overlapping-policy lint. Two earlier isolated cache-revalidation errors classified as `JWT issued at future` were recorded as the pre-release log baseline; sampled affected routes were healthy and the dossier release must verify that the error does not recur.
- **2026-08-09 dossier publication hardening:** pre-release review found that canonical program citations lacked an anonymous RLS branch, child refreshes relied only on the parent timestamp, and leaf evidence could inherit a prior leaf's entity target. The release now exposes only published-program citations backed by approved public evidence, compares complete locked child snapshots, uses a parent-first lock order across direct editing and publication, advances the parent after a direct correction, and resets the evidence entity for every leaf. The isolated role and publication regressions passed before migration; the production database results are recorded below.
- **2026-08-09 dossier migration verification:** production applied ledger versions `20260809222847` and `20260809222938` in order. Exact counts reconciled to 415 dossiers, 177 normalized current-activity values, 122 normalized participation summaries, 2,754 citations, zero active candidates, zero uncited normalized summaries, and zero activated profiles. Anonymous program and participation citations each reconciled to 127; the view, grants, invoker mode, constraints, and publisher ACLs matched the approved matrix. Security advisors remained at the five intentional deny-by-default notices plus the accepted leaked-password warning. Performance advisors retained the prior 19 unused indexes and added only the three expected, newly created dossier indexes; no overlapping-policy lint appeared.
- **2026-08-09 dossier read-path hold:** the paced crawl against application commit `aa6dc34` found six sampled `organization_dossiers` REST 500 responses and twelve paired PostgreSQL statement-timeout cancellations while null-version legacy profiles were being read. Public route responses remained available through the bounded retry/cache path, health and catalogue counts stayed consistent, and production still had zero activated profiles and zero active candidates. Promotion and enrichment were stopped pending the version-first loader hotfix and a clean repeated crawl/log gate; the verified `783b7f5` deployment remains the application rollback target while the additive dossier schema stays in place.
- **2026-08-09 dossier read-path closure:** commit `8a29b13` / deployment `dpl_4Phqy6Y3Li3EHMeRAHnyzvnUX19B` moved the version decision ahead of the rich view and restored the scoped legacy loader for all 415 currently unactivated profiles. All 11 affected routes returned their expected page, the paced crawl checked 869 pages with zero findings or recovered warnings, and Vercel/PostgreSQL recorded no hotfix-deployment 5xx, runtime error or new statement timeout. Health, the 415/379/1,219 catalogue, 2,754 citations, zero activated profiles and zero pending/approved candidates remained unchanged. The Supabase API-log connector failed during the final sample, so closure relies on the clean Postgres and Vercel feeds plus the route/crawl evidence rather than claiming that unavailable feed was inspected.
- **2026-08-08 control-plane release gate:** the complete dependency audit stopped the governance release before any push after detecting newly disclosed high-severity advisories affecting transitive `js-yaml` 4.3.0 and `nanoid` 3.3.16. The release pins their patched versions through the existing workspace override contract and requires the full release gate and production smoke before closure. No application, provider, database, authentication, editorial, or publication behaviour changes.

- **2026-07-26 pre-launch audit:** no evidence of compromise or private personal-data exposure was found. RLS was enabled on all exposed public-schema tables, anonymous access to private candidate and telemetry tables was denied, Storage boundaries and security headers were present, public invalid requests failed safely, and retention cleanup was active. The audit identified SEC-2026-001, REL-2026-001, and PRIV-2026-001 as launch blockers and recorded the remaining items above for deliberate follow-up.
- **2026-07-31 soft-beta review:** production reported healthy, the public health endpoint and primary routes returned 200, `pnpm security:validate` reported no known production vulnerabilities, and the canonical launch crawl was rerun. Supabase security advisors reported five informational deny-by-default RLS notices and two known warnings already tracked as SEC-2026-005 and SEC-2026-006. Performance advisors added the RLS-policy and index observations recorded as PERF-2026-001 through PERF-2026-003. The live response still confirmed the planned Clarity-host, framework-header, and anonymous-shell caching items.
- **2026-07-31 implementation-sequence update:** the local release candidate added deterministic complete discovery paging, same-snapshot counts, linear fallback grouping, a 5,000-marker scale gate, direct health and launch probes, and retry-warning reporting. These are source-level repairs with local regression evidence; production deployment, count consistency, cache headers, runtime logs, and the canonical crawl must still be verified before `REL-2026-003`, `REL-2026-004`, or `PERF-2026-004` can be marked deployed.
- **2026-08-02 release verification:** the complete 794-page crawl confirmed zero duplicate titles and normal route latency, but its original parallel cold-cache profile briefly produced a Supabase gateway 522 and a recovered health timeout. Immediate direct health checks returned 200 in 0.34 to 0.43 seconds, the catalogue was consistent, and all 385 published map records remained available. The validator now prewarms the compact atlas and defaults to a single 300 ms-spaced request stream so the assurance tool does not act as a load test.
- **2026-08-02 CodeQL reconciliation:** the first clean CodeQL run identified a double-unescape pattern in the private launch validator's HTML entity helper. The helper now decodes quote entities before ampersands, preventing a value such as `&amp;quot;` from being decoded twice. Workflow actions were advanced to their current supported major versions at the same time.
- **2026-08-02 production closure:** commit `d9980f0` deployed successfully to `truenorthmap.ca`; GitHub Release Validation and CodeQL passed, the complete dependency audit and 250-test release gate passed under Node 24, code-scanning and dependency-alert queues were empty, direct health and catalogue-consistency probes returned normally, and the paced 794-page production crawl completed with zero findings and zero recovered warnings. The authenticated Codex Security assessment remains the next independent assurance step.

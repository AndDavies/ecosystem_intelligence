interface:
  display_name: "TNM Evidence Mapper"
  short_description: "Enrich candidates with traceable evidence"
  default_prompt: "Use $tnm-evidence-mapper to recover and map complementary public evidence, citations, confidence, warnings, and conservative derived alignment."

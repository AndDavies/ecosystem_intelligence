interface:
  display_name: "TNM Source Discovery"
  short_description: "Find and auto-qualify durable source leads"
  default_prompt: "Use $tnm-source-discovery to enumerate a broad prospect universe, recover durable evidence, and hand every qualified lead to candidate building."

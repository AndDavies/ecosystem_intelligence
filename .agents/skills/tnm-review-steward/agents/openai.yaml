interface:
  display_name: "TNM Review Steward"
  short_description: "Validate and stage research candidates"
  default_prompt: "Use $tnm-review-steward to validate, deduplicate, and stage candidates for human review."

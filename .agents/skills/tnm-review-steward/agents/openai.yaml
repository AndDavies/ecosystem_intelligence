interface:
  display_name: "TNM Review Steward"
  short_description: "Validate and stage enriched candidates"
  default_prompt: "Use $tnm-review-steward to validate discovery yield, deduplicate candidates, and stage green and amber records directly into private Admin Review."

interface:
  display_name: "TNM Signal Refresh"
  short_description: "Find changes and refresh live records"
  default_prompt: "Use $tnm-signal-refresh to run a bounded multi-source refresh, resolve durable evidence, and stage new or existing-record candidates in private Admin Review."
